import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'eu.ra1g.cardhub',
  appName: 'cardhub',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
